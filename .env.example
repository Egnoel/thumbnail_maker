# Example environment variables for Thumbnail Maker
# Copy this file to .env and fill the values before running the proxy backend.

# Your Gemini/Google GenAI API key (keep secret, do NOT commit real keys)
API_KEY=your_real_api_key_here

# Port for the backend proxy (default 4000)
BACKEND_PORT=4000
